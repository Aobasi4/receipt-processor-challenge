import uuid
import json
import math
import datetime

# Takes in a JSON receipt (see example in the example directory) and returns a JSON object with an ID generated by your code.
def process_receipt():
    return ""

def calcuate_total_points():
    points = 0 
    return points
def character_points():
    # return len['retail'] => must be alphanumeric
    return ""
def round_dollar_amount_points():
    # if X.is_integer():
    # return 50 else return 0
    return ""
def multiple_points():
    # if X % .25 == 0: return 25 else return 0
    return ""
def item_points():
    # return (5 * math.floor(len(itenms)))
    return ""
def description_points(): 
    # if len(description) % 3 == 0: math.ceil(item_price * .2)
    # each item so you'll need a total variable 
    return ""
def day_points():
    #return 6 if the day is odd
    # day = x.strftime("%d") 
    # if day % 2 != 0: return 6 else: return 0
    return ""
def time_points(): 
    #retrun 10 points if the time of purchase is after 2 and before 4
    # hour = x.strftime("%H") 
    # minute x.strftime("%M") 
    #if hour > 2 and hour < 4:
    # if minute > 0: return 10 else return 0 
    return ""